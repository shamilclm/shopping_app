import 'package:flutter/material.dart';

class ListViewModel {
  final IconData icon;
  final String title;
  // final VoidCallback onPressed;
  ListViewModel({
    required this.icon,
    required this.title,
  });
}
